spring:
  application:
    name: Atlas-Way
  datasource:
    url: jdbc:postgresql://localhost:5432/tourism_management
    username: chael
    password: ch@el2007
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    database: postgresql
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
